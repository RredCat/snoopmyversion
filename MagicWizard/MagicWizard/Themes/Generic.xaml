<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Miracle">
  
	<Style x:Key="{x:Type local:MagicWizard}" TargetType="{x:Type local:MagicWizard}" >
		<Setter Property="Height" Value="500" />
		<Setter Property="Width" Value="500" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MagicWizard}">
					<!--<Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="500"
						>
					</Border>-->
					<Grid>
						<Ellipse Fill="Red" Opacity="0.5" Margin="20">
							<Ellipse.BitmapEffect>
								<DropShadowBitmapEffect/>
							</Ellipse.BitmapEffect>
						</Ellipse>
						<Button Margin="100" >Close</Button>
					</Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
